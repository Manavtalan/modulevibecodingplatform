// AI will overwrite this file at runtime with generated UI code.
// This is just a placeholder that loads if no AI code is provided.

export default function App() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-slate-950 text-slate-300">
      <div className="text-center space-y-3">
        <h1 className="text-2xl font-semibold">Module Preview Template</h1>
        <p className="text-sm text-slate-400">
          This is the base Vite + React + Tailwind template.
        </p>
        <p className="text-xs text-slate-500">
          When the AI runs, it will replace <code>src/App.tsx</code> and add
          components under <code>src/components</code>.
        </p>
      </div>
    </div>
  );
}
